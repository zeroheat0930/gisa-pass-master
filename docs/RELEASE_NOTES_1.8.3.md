# v1.8.3 (+41) 릴리스 노트 — 3회차 전환 퍼널판

**2026-10-18 실기 3회차 대비 업데이트.** 접수가 9/21 에 시작돼 다운로드 피크가
진행 중이라, 신규 화면 없이 기존 화면만 고쳐 작게 빨리 낸다. 목표: 9/27 제출,
10/1 이전 게시. 150분 타이머 모의고사는 +42(시험 후).

## 앱스토어 / Play 문안 (그대로 붙여넣기)

```
3회차 시험을 앞두고 첫 화면과 학습 흐름을 정리했습니다.

• 첫 화면에서 바로 AI 실전 모의고사와 학습 플랜으로 — 스크롤 없이 시작
• 복원 기출을 다 풀면 실전 환산 시간과 다음 단계(AI 모의고사)를 바로 안내
• 무료 이용 범위(하루 1회 등)를 화면마다 사실대로 표기
• 작은 화면·큰 글씨에서 버튼 제목이 잘리던 문제 수정
```

Play 출시 노트 500자 제한 안. Play 출시 이름은 `1.8.3 (41)`.

## 들어간 것

- **복원 기출 결과 화면 CTA** — 유료 퍼널 링크가 0건이던 화면에 AI 모의고사·구독
  입구. AI 진입은 `services/ai_exam_launcher.dart` 정본 하나(홈과 공유).
- **홈 첫 화면 재배치** — 유료 입구를 D-Day 바로 아래로. AI 버튼 제목 오버플로 수정.
- **카피 사실 정정 + 가격 정본** — "무료는 하루 1회 · 시험까지 D-nn"(확정 일정 &&
  60일 이내일 때만). `formatPrice` 정본. 기능표는 실제 횟수.
- **실전 환산 한 줄** — 문항 수 비례 150분 환산, 초과 시 "초과".
- **인앱 리뷰 요청** — 복원 기출 결과 화면 1곳, 정답률 ≥60% + 누적 30문항 + 90일.
- 죽은 연도 상수 3개 삭제.

## 사람이 할 것 (빌드 전·후)

1. `pubspec.yaml` `1.8.2+40` → **`1.8.3+41`** (빌드 직전).
2. `flutter analyze` 0건 · `flutter test` **371건** 통과 재확인.
3. 시뮬레이터 육안: **iPhone SE (3rd generation), iPhone 17 Pro** — AI 모의고사
   버튼이 스크롤 없이 보이는지, 큰 글씨 배율에서 오버플로 없는지.
4. 통합: `flutter test integration_test/app_test.dart -d <시뮬ID> --dart-define=SKIP_ATT=true`
5. 결제: IAP Sandbox 계정으로(개발자 기기가 이미 구매 상태면 새 Sandbox 계정) 구매 →
   배너 즉시 소멸 → 취소 → 재실행 시 재전달 없음.
6. iOS: 프라이버시 매니페스트 tracking=false + `in_app_review`·`url_launcher_ios`
   매니페스트 포함 확인(`test/privacy_manifest_test.dart` 가 검사) →
   **Transporter 검증 먼저** → 업로드.
7. Android: AAB 를 **내부 테스트 트랙**에 올려 설치본으로 리뷰 다이얼로그·결제 확인
   (사이드로드 APK 에선 리뷰 다이얼로그가 조용히 no-op).
8. **스토어 스크린샷 재촬영** — 홈 첫 화면이 바뀌어 기존 스크린샷이 앱과 다르다.
   구독 화면(기능표·히어로 문구)도 바뀌었으니 포함. 주 소구점은 홈 개편 + 결과
   화면 CTA. 필수 사이즈: iPhone 6.9" 1320×2868, 6.5" 1284×2778, iPad 13"
   2064×2752(iPad 지원 앱).
9. 심사 반려 시: 신규 의존성인 인앱 리뷰(P4)부터 빼고 즉시 재제출.

## 성공 판정

10/1~10/18 스토어 콘솔 다운로드 대비 구매 건수(직전 3.7%)가 오르는가 — 단일 지표.
애널리틱스가 없어 항목별 귀인은 불가. 리뷰 수 증감만 리뷰 요청 단독 지표.
